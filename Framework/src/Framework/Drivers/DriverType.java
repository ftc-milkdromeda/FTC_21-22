package Framework.Drivers;

/**
 * @author Tyler Wang
 * Interface to be implementated as a enum that defines a set of drivers.
 */
public interface DriverType {
    /**
     * A function for an enum that returns the ID of a specific {@link Driver}.
     * @return the driver ID for the specific {@link Driver}.
     */
    int getID();
}

//todo add way to differentiate different driver type.

